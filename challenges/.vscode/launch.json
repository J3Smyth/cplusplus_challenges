{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C/C++ Runner: Debug Session",
      "type": "lldb",
      "request": "launch",
      "args": [],
      "cwd": "${fileDirname}",
      "program": "${fileDirname}/${fileBasenameNoExtension}", // <-- THIS IS THE MAIN FIX
      "console": "integratedTerminal"
    }
  ]
}